{% extends 'gabarit.html.twig' %}

{% block title %}Complétez les billet(s) choisie(s)!{% endblock %}

{% block body %}
    <div class="container">
        <div class="py-4 text-center">
            <h2>Récapitulatif</h2>
            <p class="lead">Vous trouverez ici le récapitulatif de votre commande. N'oubliez pas d'imprimer l'email de
                la commande dés reception,
                <br>il fait office de billet!</p>
        </div>

        <div class="row">
            <div class="col-md-4 order-md-2 mb-4">
                <h4 class="d-flex justify-content-between align-items-center mb-3">
                    <span class="text-muted">Votre panier</span>
                    <span class="badge badge-secondary badge-pill">{{ command.number }}</span>
                </h4>
                <ul class="list-group mb-3">
                    <li class="list-group-item d-flex justify-content-between lh-condensed">
                        <div>
                            <h6 class="my-0">Nombre de tickets</h6>
                        </div>
                        <span class="text-muted">{{ command.number }}</span>
                    </li>
                    <li class="list-group-item d-flex justify-content-between bg-light">
                        <div class="text-success">
                            <h6 class="my-0">Code de reduction</h6>
                            <small>(si période de réduction)</small>
                        </div>
                        <span class="text-success">{{ discount }}</span>
                    </li>
                    <li class="list-group-item d-flex justify-content-between">
                        <span>Total (EUR)</span>
                        <strong>{{ command.price }}</strong>
                    </li>
                </ul>

            </div>
            <div class="col-md-8 order-md-1">

            </div>
        </div>
    </div>
{% endblock %}
{% block javascripts %}

{% endblock %}